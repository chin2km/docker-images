version: '3'

services:
  my-dind:
    build:
      context: ../
      dockerfile: ./chin2km/dind-node-builder/Dockerfile
    volumes:
      - ../:/test
    # Overrides default command so things don't shut down after the process ends.
    command: /bin/sh -c "while sleep 1000; do :; done"
